get_prop(platform_app, vendor_nothing_feature_prop)
allow platform_app vendor_hal_noth_camera_service:service_manager find;

# Allow NTCamera to read camera runtime configuration properties
# (vendor.camera.*, vendor.picture.*) — these are non-sensitive tuning values.
get_prop(platform_app, vendor_camera_config_prop)
